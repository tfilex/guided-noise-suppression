# Model weights

This directory contains the lightweight checkpoints used by the released experiments.
They are small enough to be stored directly in Git and do not require Git LFS.

The checkpoints correspond to the baseline N2D/NAFNet Light models and the tested
filter-guided hybrid variants.
